
const initialState = {
    read: false,
    starred: false,
    checked: false,
    labels: []
};

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}


export default reducer;